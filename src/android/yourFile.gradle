repositories{    
  jcenter()
  flatDir {
      dirs 'libs'
   }
}

dependencies {
	compile(name:'spiresdk-debug', ext:'aar')	
	compile(name:'commonsdk-debug', ext:'aar')	
   
	 
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}